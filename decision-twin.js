(()=>{
  /* Retired after the Decision Twin overlay caused a severe Check rendering regression.
     ExpenseIntel Check now uses the stable check/visuals.js metrics and graph layer.
     The data/evidence APIs remain unchanged; this file intentionally adds no UI. */
})();
