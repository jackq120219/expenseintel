(()=>{
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const KEY='ei_refinement_profile';
  const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'null')||{}}catch(_e){return{}}};
  const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
  function isVehicleContext(){const p=read(),t=clean([p.category,p.make,p.model,$('#check-text')?.value,$('.ei-v12-dashboard')?.textContent].filter(Boolean).join(' ')).toLowerCase();return p.category==='vehicle'||/\b(vehicle|car|porsche|bmw|mercedes|audi|ford|toyota|honda|911)\b/.test(t)}
  function refinementCount(){const p=read();return Object.entries(p).filter(([k,v])=>!['category','updatedAt','source','config'].includes(k)&&v).length}
  function addBadge(root){const meta=$('.ei-v12-analysis-meta',root);if(!meta||$('.ei-v10-refine-badge',meta))return;const n=refinementCount();if(!n)return;const b=document.createElement('span');b.className='ei-v10-refine-badge';b.textContent=`${n} refinement${n===1?'':'s'} active`;meta.appendChild(b)}
  function openRefinement(){const d=$('.ei-refine');if(!d)return;d.open=true;const cat=$('#check-category');if(cat&&isVehicleContext()&&cat.value==='auto'){cat.value='vehicle';cat.dispatchEvent(new Event('change',{bubbles:true}))}d.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>{const first=d.querySelector('select:not([disabled]),input:not([disabled])');first?.focus({preventScroll:true})},320)}
  function resultAction(){const root=$('.ei-v12-dashboard');if(!root)return;const actions=$('.ei-v12-hero-actions',root);if(!actions||$('[data-v10-refine]',actions))return;const b=document.createElement('button');b.type='button';b.className=`ei-v12-btn ei-v10-refine-btn${isVehicleContext()?' vehicle':''}`;b.dataset.v10Refine='';b.textContent=isVehicleContext()?'REFINE VEHICLE':'REFINE INPUTS';b.onclick=openRefinement;const opt=$('[data-ei-optimize]',actions);opt?.insertAdjacentElement('afterend',b)||actions.insertAdjacentElement('afterbegin',b);addBadge(root)}
  function tabAssist(){$$('.ei-v12-tabset,.check-tabs').forEach(set=>{if(set.dataset.v10Tabs)return;set.dataset.v10Tabs='1';set.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;requestAnimationFrame(()=>b.scrollIntoView({behavior:'smooth',block:'nearest',inline:'nearest'}))})})}
  function summaries(){$$('details').forEach(d=>d.classList.add('ei-v10-clean-summary'))}
  function submitState(){const form=$('[data-check-form]');if(!form||form.dataset.v10Submit)return;form.dataset.v10Submit='1';form.addEventListener('submit',()=>{form.classList.add('ei-v10-submitting');const btn=$('.check-run',form);btn?.setAttribute('aria-busy','true')},true);const out=$('[data-check-output]');if(out)new MutationObserver(()=>{if(!out.hidden&&out.children.length){form.classList.remove('ei-v10-submitting');$('.check-run',form)?.removeAttribute('aria-busy');const shell=$('.shell',out);if(shell&&!shell.dataset.v10Arrived){shell.dataset.v10Arrived='1';shell.classList.add('ei-v10-result-in');setTimeout(()=>shell.classList.remove('ei-v10-result-in'),400)}}}).observe(out,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden']})}
  function formPolish(){$$('select,input,textarea').forEach(el=>{if(!el.autocomplete&&el.tagName==='INPUT'&&el.type==='text')el.autocomplete='off'});$$('button').forEach(b=>{if(!b.getAttribute('aria-label')&&!clean(b.textContent)&&b.title)b.setAttribute('aria-label',b.title)})}
  function scan(){resultAction();tabAssist();summaries();formPolish()}
  function init(){submitState();scan();const out=$('[data-check-output]')||document.body;let q=false;new MutationObserver(()=>{if(q)return;q=true;requestAnimationFrame(()=>{q=false;scan()})}).observe(out,{subtree:true,childList:true})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();