(()=>{
  /* Retired 2026-09-08.
     The model-first lease scenario introduced a low-contrast regression and replaced
     the clearer existing graph. Keep this file as an intentional no-op so older
     cached Check HTML can still request it safely while lease-intel.js and the
     decision dashboard provide the established graph and analysis experience. */
})();