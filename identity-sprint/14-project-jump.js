(()=>{if(!location.pathname.startsWith('/project/')||document.querySelector('.ei-project-jump'))return;const modules=[...document.querySelectorAll('.lab-module')];if(!modules.length)return;const wanted=[['Utility','Utility'],['Fragility','Fragility'],['Bottleneck','Approvals'],['Undo','Lock-in'],['Evidence','Evidence'],['Failure','Failure'],['Verification','Verify'],['Action','Actions'],['Compare','Compare'],['Save','Watch']];const nav=document.createElement('nav');nav.className='ei-project-jump';nav.setAttribute('aria-label','Project Intelligence sections');const label=document.createElement('span');label.textContent='Project lens';nav.appendChild(label);let n=0;for(const [needle,name] of wanted){const m=modules.find(el=>new RegExp(needle,'i').test(el.textContent||''));if(!m)continue;const id=m.id||`ei-project-${++n}`;m.id=id;const a=document.createElement('a');a.href='#'+id;a.textContent=name;nav.appendChild(a)}if(nav.querySelectorAll('a').length<3)return;const hero=document.querySelector('.project-hero');if(hero)hero.insertAdjacentElement('afterend',nav);else document.querySelector('main')?.insertAdjacentElement('afterbegin',nav);})();