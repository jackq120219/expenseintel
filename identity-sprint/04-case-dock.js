(()=>{const I=window.EIIdentity;if(!I||document.querySelector('.ei-case-dock')||document.querySelector('.active-decision-dock'))return;const c=I.current();if(!c.title&&!c.price&&!c.location)return;const dock=document.createElement('aside');dock.className='ei-case-dock';dock.setAttribute('aria-label','Active ExpenseIntel commitment');dock.innerHTML='<div class="ei-case-dock-head"><span>Active commitment</span><button type="button" aria-label="Collapse commitment">−</button></div><strong></strong><small></small><a href="/check/">Open full Decision Passport →</a>';const refresh=()=>{const x=I.current();dock.querySelector('strong').textContent=(x.title||'Current decision').slice(0,78);dock.querySelector('small').textContent=[x.price?'$'+Number(x.price).toLocaleString('en-US'):'',x.location||'',x.category&&x.category!=='auto'?x.category.replace(/-/g,' '):''].filter(Boolean).join(' · ')||'Continue the same decision across ExpenseIntel.'};refresh();window.addEventListener('ei:case',refresh);dock.querySelector('button').addEventListener('click',e=>{const collapsed=dock.dataset.collapsed==='1';dock.dataset.collapsed=collapsed?'0':'1';e.currentTarget.textContent=collapsed?'−':'+';e.currentTarget.setAttribute('aria-label',collapsed?'Collapse commitment':'Expand commitment')});document.body.appendChild(dock);})();