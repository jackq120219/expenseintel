(()=>{const I=window.EIIdentity;if(!I||document.querySelector('.ei-commitment-bridge'))return;const path=location.pathname;const copy=[['/fairprice/','Price is one part of the commitment. A fair price can still become a bad decision if carrying cost, timing, exit or scope are weak.'],['/truecost/','True cost is the future receipt of the same commitment—not a separate estimate.'],['/costlab/','Cost structure explains what can move inside the same commitment and which inputs deserve verification.'],['/shock/','Cost Shock is a fast stress test of the same commitment, not a separate calculator. Use it to find whether secondary costs are large enough to justify deeper TrueCost work.'],['/xray/','Terms and scope convert the same commitment from a headline number into obligations, exclusions and exposure.'],['/project/','Project Intel tests whether the same commitment survives dependencies, approvals and irreversible spend.'],['/analyze/','Exact-location analysis asks what the chosen place adds to the same commitment.'],['/screen/','Location screening is an early evidence pass for the same commitment before deeper property diligence.'],['/simulate/','Simulation changes assumptions around the same project rather than creating a second project case.'],['/expense-map/','Expense Map is the alternatives lens: it tests whether geography changes the economics enough to reconsider the current commitment.'],['/find/','Find holds the operating fingerprint constant so the alternatives are comparable to the same underlying decision.'],['/compare/','Compare should change the candidate—not the project assumptions—so the Decision Passport remains apples-to-apples.'],['/timing/','Timing asks whether the same commitment improves or deteriorates when the decision date moves.'],['/forecast/','Forecast is useful only insofar as its assumptions change the same commitment you are already evaluating.'],['/watch/','Watch keeps the original commitment alive as a baseline so later changes can be judged against what you first knew.'],['/data/','Evidence is the proof layer underneath the same Decision Passport, not a separate research product.']];const hit=copy.find(([p])=>path.startsWith(p));if(!hit)return;const main=document.querySelector('main');if(!main)return;const b=document.createElement('div');b.className='ei-commitment-bridge';b.innerHTML='<strong>One commitment, not another tool:</strong> '+hit[1];const h=main.querySelector('h1');const host=h?.closest('section')||main.firstElementChild;if(host)host.insertAdjacentElement('afterend',b);})();