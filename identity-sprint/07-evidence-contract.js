(()=>{if(document.querySelector('.ei-evidence-contract'))return;const path=location.pathname;if(path==='/'||path.startsWith('/check/'))return;const main=document.querySelector('main');if(!main)return;const box=document.createElement('div');box.className='ei-evidence-contract';box.innerHTML='<strong>ExpenseIntel evidence contract</strong><p>Connected facts, your supplied facts, modeled assumptions and unresolved gaps stay separate. A deeper lens may sharpen the decision, but it does not turn an estimate into a verified fact.</p>';const first=main.querySelector('section, .container, .shell');if(first)first.insertAdjacentElement('afterend',box);else main.insertAdjacentElement('afterbegin',box);})();