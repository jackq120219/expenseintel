const NHTSA='https://vpic.nhtsa.dot.gov/api/vehicles';
function clean(v){return String(v||'').trim()}
function extractVin(text){const m=String(text||'').toUpperCase().match(/\b([A-HJ-NPR-Z0-9]{17})\b/);return m?.[1]||''}
async function decodeVin(vin){vin=clean(vin).toUpperCase();if(!/^[A-HJ-NPR-Z0-9]{17}$/.test(vin))return{ok:false,error:'No valid 17-character VIN found'};const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),6500);try{const r=await fetch(`${NHTSA}/DecodeVinValuesExtended/${encodeURIComponent(vin)}?format=json`,{signal:controller.signal,headers:{Accept:'application/json','User-Agent':'ExpenseIntel/1.0 (+https://www.expenseintel.com)'}});if(!r.ok)throw new Error(`NHTSA VIN decode ${r.status}`);const d=await r.json(),x=(d.Results||[])[0]||{};const year=clean(x.ModelYear),make=clean(x.Make),model=clean(x.Model);if(!year||!make||!model)return{ok:false,error:'NHTSA could not identify this VIN'};return{ok:true,vin,year,make,model,trim:clean(x.Trim),series:clean(x.Series),bodyClass:clean(x.BodyClass),driveType:clean(x.DriveType),fuelType:clean(x.FuelTypePrimary),engineCylinders:clean(x.EngineCylinders),engineHp:clean(x.EngineHP),displacementL:clean(x.DisplacementL),plantCountry:clean(x.PlantCountry),manufacturer:clean(x.Manufacturer),source:'NHTSA vPIC VIN Decoder',sourceUrl:'https://vpic.nhtsa.dot.gov/api/',grade:'A'}}catch(e){return{ok:false,error:String(e?.message||e)}}finally{clearTimeout(timer)}}
module.exports={extractVin,decodeVin};